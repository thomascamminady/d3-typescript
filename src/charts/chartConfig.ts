// chartConfig.ts

export const defaultChartConfig = {
    width: 1300,
    height: 400,
    marginTop: 20,
    marginRight: 20,
    marginBottom: 30,
    marginLeft: 40
};

// You can add more configurations if needed
export const anotherChartConfig = {
    // ... another set of configurations
};
